[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    integration: tests that need a live ircu2 (docker harness)
filterwarnings =
    ignore::DeprecationWarning
